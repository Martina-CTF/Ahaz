[project]
name = "ahaz_common"
version = "0.1.0"
description = "Type definitions for Ahaz"
dependencies = ["pydantic>=2.12.3"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
